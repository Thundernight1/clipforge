# ClipForge

Own-brand freemium web app: **long video → short clips in minutes**.

Uses **Ollama Cloud** (`kimi-k3:cloud`) via Netlify function `/api/generate-clips`. API key never reaches the browser.

Live: https://clipforge-tn1.netlify.app

See DEPLOY.md and `.env.example`.
